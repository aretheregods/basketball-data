# Force building native modules (like sqlite3) from source so they are compiled against the local system's glibc version.
# This prevents GLIBC compatibility errors (e.g., GLIBC_2.38 not found) on older Linux distributions like Debian.
build-from-source=true
